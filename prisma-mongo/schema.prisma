generator client {
  provider = "prisma-client-js"
  output   = "../node_modules/@internal/mongo-client"
}

datasource db {
  provider = "mongodb"
}

model Log {
  id   String @id @default(auto()) @map("_id") @db.ObjectId
  data String
}